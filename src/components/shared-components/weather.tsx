import {Box, BoxProps, styled} from "@mui/material";

const WeatherWrapper = styled(Box)<BoxProps>(({  }) => ({
    width: 850,
    margin: "3em auto",
    padding: '2rem'

}));

export default WeatherWrapper
